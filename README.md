
  # Coffee Bean Tracker App

  This is a code bundle for Coffee Bean Tracker App. The original project is available at https://www.figma.com/design/OFepvcxlGWNkMZ8jsCFjon/Coffee-Bean-Tracker-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  